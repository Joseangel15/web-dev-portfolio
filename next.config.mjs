/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    // Add other configurations as needed, e.g., base path for GitHub Pages
    // basePath: '/your-repo-name', 
};
export default nextConfig;